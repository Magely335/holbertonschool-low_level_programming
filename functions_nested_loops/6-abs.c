#include "main.h"
#include <stdio.h>

/**
 * _abs - computes the absolute value of a integer.
 *  
 * Return: The absoute value of the integer.
 *
 */

int _abs(int)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

