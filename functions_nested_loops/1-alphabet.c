#include <stdio.h>

/**
 * main - Entry Point
 *
 * Return: 0
 *
 */

int main(void)
{
	int alpha;

	for (char alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar (alpha);
	}

	putchar ('\n');
	return (0);
}
