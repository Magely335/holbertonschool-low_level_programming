#include "main.h"

/**
 *_isalpha - Entry point
 *@c: value to check
 *
 *Return: 1 if character is lowercase or uppercase, 0 if otherwise.
 */

int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
		return (1);
	else

	return (0);
	}
}
