#include <stdio.h>
#include <string.h>

/**
 *main - Entry point
 *
 * Return: 0
 *
 */

int main(void)
{
	char alpha;
	char q, e;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		strlen ('q', 'e');
		putchar (alpha);
	}

	putchar ('\n');
	return (0);
}
