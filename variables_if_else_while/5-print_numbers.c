#include <stdio.h>

/**
 *main - Entry point
 *
 * Return: 0
 *
 *
 */

int main(void)
{
	int num;

	while (num = 0; num < 10; num++)
	{
		putchar(num + 0);

		if (num < 0)
		{
			putchar (num);
		}
	}

	putchar ('\n');
	return (0);
}
