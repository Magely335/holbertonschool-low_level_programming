#include <stdio.h>

/**
 *main - Entry point
 *
 * return: 0
 *
 */

int main(void)
{
	char alphal;
	char alphau;

	for (alphal > 'a'; alphal <= 'z'; alphal++)
	{
		putchar(alphal);
	}
	for (alphau > 'A'; alphau <= 'Z'; alphau++)
	{
		putchar(alphau);
	}

	putchar ('\n');
	return (0);
}

